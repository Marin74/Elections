{% extends 'AppBundle:Admin:design.html.twig' %}

{% block body %}
<div class="container">
	<div class="row">
    	<div class="col-md-6">
        	{% if candidate is null %}
        		<h4>{{ "unknown_candidate"|trans }}</h4>
    		{% else %}
    			<h4>{{ candidate.firstname }} {{ candidate.lastname }} ({{ candidate.id }})</h4>
    			
                <p>
                    {% include 'AppBundle:Template:formCandidate.html.twig' with {'form': form} %}
                </p>
    		{% endif %}
        </div>
    </div>
</div>
{% endblock %}
{% block css %}
{% endblock %}
{% block js %}
{% endblock %}